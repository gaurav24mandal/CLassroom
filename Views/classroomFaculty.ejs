<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Classroom dashboard</title>
    </head>
    <body>
        <h1>Classroom dashboard</h1>
        <h2>Name: <%= classroom.name %></h2>
        <p>Description: <%= classroom.description %></p>
        <p>Classroom ID: <em><%= classroom._id %></em></p>
        <p>Students can join this classroom using the above Classroom ID.</p>
        <p style="color: red;"><%= message %></p>
        <p>Found a bug? <a href="mailto:bluefoxdevs@gmail.com">Inform</a> the developer.</p>
        <hr>
        <h2>Post assignments</h2>
        <form action=<%= "/assignment/"+classroom._id %> method="post">
            <input type="text" name="assignmentTitle" placeholder="Title"><br>
            <textarea name="assignmentDescription" placeholder="Description" cols="30" rows="10"></textarea><br>
            <button type="submit">Post</button>
        </form>
        <h2>List of assignments</h2>
        <% for(let i=0; i<assignments.length; i++) { %>
            <hr>
            <h3><%= i+1 %>. <%= assignments[i].title %></h3>
            <p><%= assignments[i].description %></p>
            <a href=<%= "/submission/viewAll/"+assignments[i]._id %>>View submissions</a>
        <% } %>
    </body>
</html>